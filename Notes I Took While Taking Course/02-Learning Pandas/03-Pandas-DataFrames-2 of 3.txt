Pandas - DataFrames, Lecture 2 of 3

my_dataFrame > 0:
(Very similar to doing it to a numPy array)
-- goes Element-by-Element, and returns a new dataFrame, doing that condition
For example, it would return
     W      X     Y
A  True   True  False
B  False  True  False
C  False  True  True

bool_dataFrame = my_dataFrame > 0
is possible


So if we do:
my_dataFrame[my_dataFrame > 0] or, using the variable from above:
my_dataFrame[bool_dataFrame]
We would get values where it is True, and NaN where it is False
     W     X     Y
A   2.7   0.6   NaN
B   NaN   0.7   NaN
C   NaN   1.1   1.7

# Let's Skip the NaNs and only get the values that match what we are look for.

# When you're passing conditions based on Series values
my_dataFrame[my_DataFrame['W']>0]
---return all rows where column W is >0


my_dataFrame[my_DataFrame['Z'] < 0]
Should just return row C because thats the row where column Z is <0.


results_dataFrame = mydataFrame[my_dataFrame['W']>0]
results_dataFrame['X'] gets the X column of the results

I could also do:
my_dataFrame[my_dataFrame['W']>0]['X']
--get rows where W column is bigger than 0, specifically just the X column

my_dataFrame[my_dataFrame['W']>0]['W,'Y']
-- gets rows where W column is bigger than 0, specifically just the W and Y columns
Line above is same as:
bool_series = my_dataFrame['W']>0
result = my_dataFrame[bool_series]
my_cols = ['W','Y']
result[mycols]


Multiple conditions (for Pandas):
# Pass each condition in its own set of parentheses, and separate vai the "&" (aka "and") and "|" (aka "or") operators
& for "and"
| for "or"
my_dataFrame[(df['W'] > 0) & (df['Y'] > 1)]


my_dataFrame.reset_index() <-moves the existing indexes to a new column called "index" and the actual indexes become a list of numbers, starting at 0 aka 0,1,2,3,4
Note: It does NOT occur in-place. To make it happen in place
my_dataFrame.reset_index(inplace=True)

   index  W  X
0    A    2  2
1    B    5  3
2    C    8  1
3    D    2  0


One more index example: setting the index.

newIndex = 'CA NY WY OR CO'.split()  # Remember, .split() splits into an array of strings via the empty space
Now new_index is: ['CA', 'NY', 'WY', 'OR', 'CO']

# New Column
#New Column, #NewColumn, #Adding a Column, #AddingAColumn, #Adding a New Column, #AddingANewColumn

my_dataFrame['New Column'] = new_index  <-This is an example of creating a brand new column
my_dataFrame['States'] = new_index  # <- This will create a new column onto the existing dataFrame with column name "States", and the values = to the new_index array I had created above.

Now, to set that column to become the index for that column.
my_dataFrame.set_index('States')

To Review, we learned:
--Conditional Selection (similar to NumPy selections)
--If we pass in a Series (aka the collumn







